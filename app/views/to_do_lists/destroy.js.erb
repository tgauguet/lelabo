$("#list-list").html( "<%= escape_javascript(render 'to_do_lists/lists') %>")
$(".count-tasks").html( "<%= escape_javascript (render 'tasks/count') %>")

// on ajax delete lists action
$(".delete-pls").bind("ajax:success", function(){
	$(this).closest(".to-do-cards").fadeOut();
});
