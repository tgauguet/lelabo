$(".qttt").html("<%= escape_javascript (render 'recipes/quantities', recipe: @recipe, first: @first, second: @second, third: @third, fourth: @fourth, totals: @totals) %>");
$(".popover-bck").html("<%= escape_javascript (render 'recipes/quantities_pop', recipe: @recipe) %>")

// **************** QUANT & COST ACTION ******************
  // Hide and show select measure popover
    $(".q-f1btn").hide();
    $(".q-f2btn").show();
    $(".qt-pop").hide();
    $(".popover-bck").hide();
    $(".pop-on").click(function(){
      $(".pop-qt").show();
      $(".actions-cntnr").addClass("no-scroll");
    });
    $(".pop-off").click(function(){
      $(".select-measure").hide();
      $(".actions-cntnr").removeClass("no-scroll");
    });
  // hide and show forms in select unit popover
    $('.trigger').click(function() {
      $('.content').hide();
      $('.' + $(this).data('rel')).show();
    });
  // Hide and show form informations in select quantities forms
    $('.trigger2').click(function() {
        $('.content2').hide();
        $('.' + $(this).data('rel')).show();
    });
  // hide and show each cells
  // hide and show each cells
    $('.q-f1btn').click(function() {
      $('.q-f1').show();
      $(".q-f2").hide();
      $(".q-f1btn").hide();
      $(".q-f2btn").show();
    });
    $('.q-f2btn').click(function() {
      $('.q-f2').show();
      $(".q-f1").hide();
      $(".q-f2btn").hide();
      $(".q-f1btn").show();
    });
  // Hide and show loading animation for each page changement inside recipes form
      $(".recipe-btn").click(function(){
        $(".rec-cntnr").hide();
        $(".load-url").show();
      });
