<% title "Renvoyer les instructions de confirmation de mon email" %>
<div id="add-email">
<h2>Renvoyer les instructions de confirmation</h2>

<%= form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.email_field :email, autofocus: true, placeholder: "Email", id: "inscription_field4" %>
  </div>

  <div class="actions">
    <%= f.submit "Envoyer à nouveau" %>
  </div>
<% end %>

