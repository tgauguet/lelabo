<div class="star">
  <div class="class-helper inline_divs">
		<% unless assembly.category.nil? %>
			<div class="circle <%= category_color(assembly) %>"></div>
		<% end %>
      <div class="line-cntnr">
        <%= link_to edit_assembly_path(assembly) do %>
          <span class="ing-name float">
            <%= truncate(assembly.title.humanize, length: 25) %>
          </span>
        <% end %>
        <span class="ing-price">
          <span class="ing-img">
            <%= form_for assembly, remote: true do |f| %>
                <%= f.hidden_field :stared, value: update_stared_item(assembly) %>
                <% if assembly.stared %>
                  <%= image_submit_tag "star.png", class: "stared-item" %>
                <% else %>
                  <%= image_submit_tag "missing-star.png", class: "stared-item" %>
                <% end %>
            <% end %>
          </span>
          <span class="ing-img2">
            <%= form_for assembly, remote: true do |f| %>
                <%= f.hidden_field :loved, value: update_loved_item(assembly) %>
                <% if assembly.loved %>
                  <%= image_submit_tag "heart-2.png", class: "stared-item2" %>
                <% else %>
                  <%= image_submit_tag "heart.png", class: "stared-item2" %>
                <% end %>
            <% end %>
          </span>
  			</span>
        <span class="ing-unit">
  				<%= assembly.category %>
  			</span>
        <%= image_tag "delete-sml.png", class: "delete-rcp delete-pop-over delete-tsk-btn delete-down" %>
        <%= image_tag "cancel.png", class: "show-delete remv-delete delete-rcp delete-tsk-btn delete-down", style:"display:none;" %>
        <%= link_to assembly_path(assembly.id), method: :delete, id: "delete-task" do %>
          <%= image_tag "checked-3.png", class: "show-delete delete-rcp delete-tsk-btn delete-down", style:"display:none;" %>
        <% end %>
    </div>
  </div>
</div>
