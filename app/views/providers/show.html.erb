<% title "#{@provider.name.humanize}" %>
<div class="actions-cntnr">
	<div class="action-cntnr">
		<%= @provider.name %><br/>
		<%= @provider.city %>
	</div>
	<%= link_to "Modifier", edit_provider_path(@provider) %><br/>
	<%= link_to "Index", providers_path %>
</div>